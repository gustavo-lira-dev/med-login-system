# Rename this file exactly to ".env" and change the variables to the desired values
# it should make the docker-compose.yml work with the correct attributes.

# Database Credentials
DB_NAME=data_base
DB_USER=db_user
DB_PASSWORD=password

# Database Port
DB_PORT=5432
API_PORT=8080